# MapConductor Icons US

United States map glyphs for `com.mapconductor:icons`. Selection is explicit.
Original 24x24 source artwork is stored in `artwork/`.

```kotlin
PinGlyphIcon(glyph = UnitedStatesMapIcons.interstate)
```
